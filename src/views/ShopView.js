import React from 'react';

// internal imports
import { Shop, View } from '../config/lib';

export const ShopView = ({}) => {
  return (
    <View>
      <Shop title={'Vegetables'} />
    </View>
  )
}
